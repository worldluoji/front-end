{
  "name": "lowcode-demo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:engine": "pnpm -r --filter=./packages/engine run dev",
    "build:engine": "pnpm -r --filter=./packages/engine run build",
    "preview:engine": "pnpm -r --filter=./packages/engine run preview"
  }
}
